# 复制为 local.env 后填写。local.env 不得提交到代码库。
TRANSCODE_AGENT_INTERNAL_TOKEN=请填写随机生成的内部服务Token
# 可选：内部服务调用时记录的工号。
TRANSCODE_AGENT_INTERNAL_EMPLOYEE_ID=

# 本地客户端连接已初始化的共享库时设为 false，避免启动时重置他机任务或规则。
# 独立部署默认保留原有启动维护行为。
APP_STARTUP_MAINTENANCE_ENABLED=true
