# 全平台 PostgreSQL 迁移 P8 业务验收报告（2026-08-27）

> 对应计划：`全平台统一PostgreSQL实际迁移执行计划.md`。
> 环境：开发 PostgreSQL 统一配置已启用。
> 原则：自动验收不发邮件、不调用 IMAP、不提交录单、不生成业务测试数据；所有外部副作用流程保留给业务确认。

## 1. 自动验收结论

| 验收项 | 结果 | 证据 |
|---|---|---|
| 正式统一配置 | 通过 | 五个模块均解析到同一 PostgreSQL；旧模块级运行变量未参与正式启动 |
| Flask 实际启动 | 通过 | 本机 5000 端口监听，`GET /` 返回登录跳转 302 |
| 应用工厂与路由 | 通过 | 应用工厂成功，加载 154 条路由；启动前后 7 张关键表行数一致 |
| 登录态页面 | 通过 | 22 个核心只读页面均返回 200，覆盖工作台、客户档案、规格维护、订单分流、订单详情、内销模板、邮件回复、接口维护、任务、历史、反馈、规则中心和邮件管理 |
| 模块服务读取 | 通过 | 身份、客户、邮件订单、PP 转码、AI 配置、个人任务服务均从 PostgreSQL 读取成功 |
| 最小写入验证 | 通过 | 现有系统设置原值写回并复核一致；未新增业务记录或改变业务语义 |
| 附件路径与下载 | 通过 | 1,895 个邮件原文/附件索引文件校验失败为 0；抽取一个真实订单附件通过页面下载，HTTP 200 |
| 回退演练 | 通过 | `scripts/run_platform_legacy_rollback.py` 可让平台域回到 SQLite、订单自动化回到本机 Docker PostgreSQL；4 个核心页面返回 200，读取订单案例 866 条 |

## 2. 已覆盖的登录态页面

以下页面在工号 `23582` 的测试登录会话下返回 HTTP 200：

- 工作台、我的设置、客户档案、客户规格维护；
- 订单邮件分流、分流规则、订单邮件详情、内销录单模板、邮件回复草稿页、接口维护；
- 工作规划、历史记录、反馈；
- 通用规则、转码规则、PP 规则、转码规则中心、转码 Agent 模型配置；
- 邮件管理首页、邮箱账号页、抓取批次列表及一个已有抓取批次详情。

## 3. 需要 ny 进行的人工业务验收

这些步骤带有真实外部副作用或会改变业务记录，不能由自动迁移脚本替代：

执行时请在 `全平台迁移P8人工业务验收记录.md` 逐项留存测试对象、时间、结果和异常处置；未取得测试数据与外部发送授权时，不执行相应步骤。

| 流程 | 操作 | 通过标准 |
|---|---|---|
| 登录与权限 | 用正常账号登录、退出；以非接口管理员账号访问接口维护 | 正常账号可用；非管理员被拒绝接口维护 |
| 邮件同步 | 选一个允许测试的邮箱执行“同步新邮件” | 不重复入库；抓取任务、邮件列表和附件均可追溯 |
| 客户识别/分流 | 选择待确认邮件，刷新客户识别并保存一次业务分类 | 客户归属、操作人和分流事件正确保存 |
| 订单抓取 | 选择一封允许测试的订单邮件执行“订单抓取及确认” | 后台任务状态可见，模板生成且不阻塞页面 |
| 料号查询与回调 | 在 Mock 配置下执行一次批量料号查询；如有创建中记录，执行 Mock 回调 | 料号与品名按接口返回回填，接口交互和流程记录可追溯 |
| 提交录单 | 对确认无误的测试订单执行 Mock 提交 | 状态变为已完成；除下载 Excel 外其余编辑/接口按钮禁用 |
| 邮件回复 | 先保存草稿；待 SMTP 配置和收件人确认后发送测试邮件 | 草稿可恢复；发送记录、关联订单数据和收件人正确 |
| DBeaver 抽查 | 抽查客户、邮件、订单案例、模板和接口调用日志 | 页面数量与 PostgreSQL 查询结果一致 |

## 4. 已执行的真实流程验收（禁止发送邮件）

在 ny 授权“可测试邮件、不得发送邮件”后，已完成以下真实流程验证：

| 流程 | 结果 | 证据 |
|---|---|---|
| IMAP 同步 | 通过 | 抓取任务 #10 完成：2 封新增、0 重复、2 条订单案例；IMAP 只读抓取，未改变邮件已读状态。 |
| 客户刷新与分流 | 部分通过 | 已刷新当日 2 封邮件；因无客户档案匹配，二者保持未匹配。案例 #296748 的人工分流审计已生成。 |
| 订单抓取及确认 | 通过 | 后台任务 #11 完成，生成模板 #14 与 2 条明细，页面操作不等待文档提取。 |
| 料号 Mock 与回调 | 通过 | 先完成命中与“创建料号中”两条分支，再以 Mock 回调回填；2 个料号任务均为 resolved，接口日志 2 条。 |
| Mock 录单提交与锁定 | 通过 | 案例状态为 archived/completed/submitted；模板页面的重新提取、保存与两类接口按钮进入锁定，下载 Excel 保留。 |
| 回复草稿 | 部分通过 | 虚拟收件人草稿可保存并恢复；未调用 SMTP、未发送邮件。 |

详细逐项记录见 `全平台迁移P8人工业务验收记录.md`。

## 5. 观察期安排

- P9 观察期从开发人员以当前正式配置实际使用服务开始计算，建议连续 3–7 天；
- 每日检查 PostgreSQL 连通性、Flask/Worker 错误、邮件抓取、附件下载、订单流程与新增数据抽查；
- SQLite、Docker PostgreSQL、附件文件及 P3 备份在观察期内只读保留，不删除、不覆盖；
- 只有第 3 节人工验收无阻塞问题且观察期无未解释差异，才能将整个迁移计划收口。
