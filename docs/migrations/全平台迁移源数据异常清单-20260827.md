# 全平台迁移源数据异常清单（2026-08-27）

> 对应执行计划：`docs/migrations/全平台统一PostgreSQL实际迁移执行计划.md`。
> 来源快照：`backups/platform-migration/20260827T114512/source-sqlite.sqlite3`。
> 原则：原始备份不改动；异常行不伪造关联、不写入错误的目标外键；每项均须可复核。

| 异常编号 | 来源域 | 表 | 行标识 | 问题 | 迁移处理 | 原始保留位置 |
|---|---|---|---|---|---|---|
| EXC-001 | 非自动化转码 | `pp_transcode_confirmation_items` | `id=1,2,3`；分别引用 `job_id=33,34,35` | 源 `jobs` 表不存在对应任务，无法满足 PostgreSQL 外键 | 显式隔离，不迁移这 3 条历史确认项；其余转码数据正常迁移 | SQLite 冻结快照 |
| EXC-002 | 旧 SQLite 自动化副本（非权威来源） | `mail_attachment_texts` | `attachment_id=772` | 不存在对应附件索引 | 不纳入迁移。邮件/附件自动化域以 Docker PostgreSQL 为唯一来源；该 SQLite 历史副本仅随快照保留 | SQLite 冻结快照 |

## 审计结论

- SQLite `PRAGMA foreign_key_check` 发现 1 条异常，对应 EXC-002。
- 额外按目标 PostgreSQL 外键复核，发现 EXC-001 的 3 条未声明 SQLite 外键的历史孤儿记录。
- 除上述项目外，已检查的转码任务相关表未发现引用不存在 `jobs.id` 的记录。

## 执行约束

1. 迁移脚本默认严格复制；仅在传入 EXC-001 的明确行标识时才跳过这 3 行。
2. 不得将它们改挂到其他任务、补造任务 ID 或删除来源数据。
3. 若后续确认原任务可恢复，须走独立的数据修复单并生成新的迁移批次，不覆盖本次快照留痕。
