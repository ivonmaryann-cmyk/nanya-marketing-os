# 本地切换共享开发库记录

- 执行日期：2026-08-31，负责人：Codex。
- 执行范围：当前本地工作区连接切换；不部署服务器、不推送 Git、不导入本地测试数据。
- 代码基线：dev / 0df1111；包含既有未提交改动，本次也未提交。
- 状态：本地连接已切换，连接池与登录页验证通过；未执行外部订单/料号接口。

## 配置与结构

- 统一连接切换到用户指定的共享 PostgreSQL 开发库，自动化、身份、转码、配置、规划均使用同一数据库。
- 修改仅存于被 Git 忽略的 `config/local.env`，不在本记录中保存密码或连接串。
- 涉及变量：`PLATFORM_DATABASE_BACKEND`、`PLATFORM_DATABASE_URL`、`PLATFORM_DATABASE_READ_WRITE_ENABLED`。
- 旧模块级变量 `AUTOMATION_DATABASE_URL`、`IDENTITY_DATABASE_URL`、`TRANSCODE_DATABASE_URL`、`CONFIG_DATABASE_URL`、`PLANNING_DATABASE_URL` 同步更新，避免配置回退时混用。
- 共享库无缺失表字段，现有 10 个自动化迁移文件均已登记，校验值一致，包含 `0009_mail_account_owner_email_unique.sql`。
- 本次未执行 DDL、迁移 CLI、数据复制或清理，无需另行将数据库结构迁移到共享库。
- `external_task_id` 继续使用已有任务字段，新建料号页面不需要新增表列。

## 启动保护

现有启动逻辑根据本机 PID 判断任务是否中断，并在本地缺少规则文件时初始化启用规则；这些动作不适合多电脑共享数据库。

- 增加 `APP_STARTUP_MAINTENANCE_ENABLED` 开关，本机配置为 `false`。
- `create_app()` 仍建立 Flask 应用、注册页面、检查核心表并准备本地目录，但跳过启动维护、规则播种、默认版本重设及中断任务修复。
- 未配置该变量的独立部署保留原有默认行为。
- 此开关只约束启动维护，不代表所有业务页面都只读，也不解决跨电脑后台任务协调或文件共享问题。
- 修改文件：`fangzheng_web_app/__init__.py`、`config/local.env.example`、`tests/test_shared_database_startup.py`。

## 验证证据

- `python -m unittest tests.test_shared_database_startup -v`：2 项通过，分别验证默认行为与共享库客户端跳过维护。
- 五个模块使用实际连接池执行只读 SQL，均确认连接到指定共享库。
- 8 张关键表的 SELECT/INSERT/UPDATE 权限检查通过；启动前后记录数一致。
- 本地服务保持 `http://127.0.0.1:5001`，`GET /login` 返回 HTTP 200。
- 详细只读证据位于被 Git 忽略的 `storage/migration_reports/shared_switch_20260831_preflight.json` 与 `shared_switch_20260831_verified.json`。
- 未登录后逐页执行完整业务验收；未调用生成订单、新建料号或邮件发送接口。

## 回退

原配置已备份为 `config/local.env.pre-shared-20260831_075934.bak`，文件被 Git 忽略。

仅在确定要回到本机测试数据源时，先停止当前本地应用，再执行：

```powershell
cd D:\Carson\nanya-marketing-os
Copy-Item -LiteralPath config/local.env.pre-shared-20260831_075934.bak -Destination config/local.env
python -m flask --app app run --host 127.0.0.1 --port 5001
```

回退只改变连接，不把共享库的新写入复制回本地；两边数据库均保留。若终端显式设置过数据库环境变量，需先清除对应覆盖项后再启动。

## 后续注意

- 本地测试库数据未合并到共享库，共享库原有数据保持现状。
- `storage/`、上传文件和附件未搬迁，同事新增记录引用的文件可能不在本机。
- 连接共享库后，页面保存、同步邮件、接口维护等业务操作会作用于共享数据。
- 现有其他未提交代码、数据库迁移文件与本次无关的改动均保留，未强推、未重置分支。
