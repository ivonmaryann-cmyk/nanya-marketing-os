# 全平台迁移前置审计（2026-08-27）

> 迁移批次：`20260827T114512`
> 目的：P3 冻结时点的来源快照、文件索引和目标库基线留痕。
> 本次只创建备份和只读审计，尚未在目标库创建表或复制业务数据。

## 1. 冻结观察

- 本机 `127.0.0.1:5000` 当时未监听；
- 未识别到本项目 Flask、邮件同步、订单抓取或转码 Worker 进程；
- 本机 Docker PostgreSQL 容器健康运行；
- 本次备份期间未执行来源库写入操作。

## 2. 一致性来源备份

| 来源 | 备份文件 | 验证 | SHA256 |
|---|---|---|---|
| SQLite `storage/app.db` | `backups/platform-migration/20260827T114512/source-sqlite.sqlite3` | SQLite `PRAGMA integrity_check=ok` | `57e827aeafc7f33b9cccfcc7e3989a12750ea4a43c67fa0c94a379ef9f42c500` |
| 本机 Docker PostgreSQL `nanya_automation` | `backups/platform-migration/20260827T114512/source-automation-postgres.dump` | 容器内 `pg_restore --list` 成功 | `7f6452c884bab935f122b3e0ee77d0bd149e94ba90de077f7686ccdbcedfb4b7` |

备份保留期至少 30 天，未经 ny 确认不得删除、覆盖或提交到 Git。

## 3. 文件索引审计

订单自动化权威来源中的 `mail_attachments` 共 1,029 条：

| 检查项 | 结果 |
|---|---:|
| 附件索引总数 | 1,029 |
| 有 `stored_path` 的索引 | 1,029 |
| 有 `sha256` 的索引 | 1,029 |
| 索引累计 `size_bytes` | 198,747,474 bytes |

本次迁移只复制索引字段（路径、大小、哈希等），不移动附件、邮件原文、Excel/PDF 文件本体；文件本体继续沿用现有存储路径。

## 4. 目标库基线

- 数据库连接、建表权限和 `public` schema 创建权限均已验证；
- `public` schema 表数为 0；
- 因此可以按版本化 DDL 创建全平台 Schema；
- 此时点未向目标库写入任何表或业务数据。

## 5. P4 放行结论

来源备份可读、来源静态快照已完成、目标库为空且权限通过。允许进入 P4 执行 Schema 创建；完成 P4 后仍需先做结构核对，再进入 P5 数据复制。
