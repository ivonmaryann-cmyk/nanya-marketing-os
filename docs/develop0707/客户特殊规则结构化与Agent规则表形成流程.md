# 客户特殊规则结构化与 Agent 规则表形成流程

日期：2026-07-07
最近更新：2026-07-13
适用范围：营销转码 Agent 的客户特殊规则结构化、业务确认、Agent 专用规则源形成。
原则：本文描述规则形成流程、维护口径和当前已实现运行边界。

## 2026-07-13 运行分流补充

```text
确定性规则：草稿确认 -> 最终Agent机器规则/辅助映射 -> 规则引擎执行。
模型语义：草稿的模型语义候选 -> 受控JSON标准化 -> 业务批准 -> 独立语义规则 -> 规则引擎执行。
模型不直接生成或覆盖制造码。
业务原表后续可新增“订单备注”列；该列为可选字段，缺失时记录missing_input，不允许模型猜测。
已完整生成但存在业务风险的候选码继续显示，并以红色和人工确认提示区分；只有基础必需字段解析失败才不出码。
准确率将绿色和红色已出码统一计入分母；颜色只表示是否需要人工确认。
导出状态列固定为：结果对比、转码状态、人工确认提示。`结果对比`中已生成码为TRUE、未生成码为FALSE、PP/RC行为“跳过”。
```


## 0. 2026-07-08 简化同步后的最新口径（本次更新）

本次已根据业务反馈和最新处理要求，对结构化草稿进行简化同步。后续业务确认和生成最终 Agent 规则表时，优先使用 Excel 新增 Sheet：

```text
结构化草稿_简化
清洗日志
简化统计
```

原 Sheet `结构化草稿`、`待确认降噪说明`、`业务反馈处理说明` 保留用于追溯，不作为后续生成最终 Agent 规则表的主输入。

### 0.1 当前仍保持“一客户一行”

当前阶段仍是“业务原表 -> 结构化草稿”的中间草稿，不是最终 Agent 执行规则表。因此本次仍保持：

```text
一个客户代码 / 一个客户简称 = 一行
```

最终 Agent 可执行规则表后续再从简化草稿拆成“一条规则一行”。

### 0.2 本轮只保留当前营销转码需要的字段

`结构化草稿_简化` 中，结构化规则只保留以下字段：

```text
胶系
基板厚度
铜箔规格
基板尺寸
胶水类别
铜箔类型+印字/非印字
基板级别
总/芯厚
```

对应可保留覆盖字段：

```text
glue_code
thickness_rule
thickness_code
copper_code
size_code
glue_category_code
copper_type_code
grade_code
tc_code
```

### 0.3 本轮明确不形成执行规则的内容

以下内容不进入 `通用特殊规则_结构化` / `CCL特殊规则_结构化` 的可执行部分：

```text
组合结构
铜箔厂商
玻布厂商
配方代码
PP规则
PP小片规则
订单语义规则
排版结构/布号映射
非影响转码备注_结构化
```

处理方式：

- `组合结构`、`铜箔厂商`、`玻布厂商`、`配方代码`：移入 `不纳入本次转码说明`。
- `PP特殊规则`、`PP小片特殊规则`：本轮不解析、不执行。
- `订单备注`、`订单内容`、`客户订单字段`、`未标TG`、`备注第5码`、`双面字样` 等订单语义：本轮不进入客户特殊执行规则，后续单独做订单语义模块。
- `非影响转码备注`：只保留原文参考，不再拆成规则化字段，不进入执行规则。

### 0.4 状态简化

本次简化后，主用 Sheet `结构化草稿_简化` 只使用以下状态：

```text
可执行草稿
技术待支持
不纳入
无规则
```

含义：

| 状态 | 含义 |
|---|---|
| 可执行草稿 | 当前字段范围内存在可形成规则的内容 |
| 技术待支持 | 业务规则清楚，但当前 Agent 执行能力尚未支持，暂不进入最终执行规则 |
| 不纳入 | 本轮不转、仅参考、非影响备注、PP/PP小片、组合结构、订单语义等 |
| 无规则 | 当前客户无可结构化执行规则 |

当前 `有待确认` 不再出现在主用简化 Sheet 中。如后续业务新增未确认事项，可以重新增加该状态，但当前不使用。

### 0.5 最新统计

本次简化同步后，`结构化草稿_简化` 统计如下：

```text
可执行草稿：66
技术待支持：19
不纳入：37
无规则：97
合计：219
```

清洗指标：

```text
原结构化内容含“状态=待确认/确认点=”的客户行：70
简化结构化列中剩余待确认文本：0
简化结构化列中剩余非本轮字段：0
清洗日志条目：295
```

### 0.6 优先级最新口径

如果同一字段存在冲突，按以下优先级处理：

```text
客户订单明示 > CCL特殊规则 > 通用特殊规则 > 普通规则 > 默认规则
```

说明：

- CCL 特殊规则比通用特殊规则更具体，因此在同一字段冲突时优先。
- 业务反馈列作为最高可信来源，用于修正原始规则中的多值、冲突和已废弃规则。
- 普通规则和默认规则不能覆盖客户特殊规则。
- 当前草稿只清洗规则，不直接生成最终 Agent 执行规则。

### 0.7 本次新增 Sheet 说明

| Sheet | 作用 |
|---|---|
| 结构化草稿_简化 | 后续业务确认和生成最终 Agent 规则表的主输入 |
| 清洗日志 | 记录从原结构化内容中移出的字段、原因和处理方式 |
| 简化统计 | 记录本次同步口径、状态统计和清洗指标 |

## 1. 背景

业务提供的新格式客户特殊需求原表用于提升营销转码 Agent 的客户特殊规则命中率。

当前目标不是直接替换项目原有 `transcode_rules.xlsx` 的 `特殊需求` Sheet，而是形成一套 Agent 专用规则源：

```text
业务原表
  -> 结构化草稿
  -> 业务确认
  -> Agent 可执行客户特殊规则表
  -> 营销转码 Agent 读取
```

这样可以避免影响普通转码、旧规则解析和其他功能。

## 2. 当前涉及文件

业务提供原表：

```text
docs/develop0707/客户特殊清单新.xlsx
```

板厚换算补充表：

```text
docs/develop0707/超颖客户下单尺寸 特殊要求(1).xlsx
docs/develop0707/健鼎客户下单特殊要求(1).xlsx
```

当前结构化草稿：

```text
docs/develop0707/客户特殊规则结构化草稿_按原表行_20260708.xlsx
```

规则引擎代码参考：

```text
fangzheng_web_app/transcode_engine.py
fangzheng_web_app/transcode_agent_rules.py
fangzheng_web_app/transcode_agent_service.py
```

当前项目规则表参考：

```text
storage/rules/transcode/versions/transcode_bootstrap_20260703_191711/transcode_rules.xlsx
```

规则表关键 Sheet：

```text
胶系代码
胶系类别
编码规则
特殊需求
总芯厚转换
客户下单与胶系基板转换
```

## 3. 业务原表字段

业务原表 Sheet：

```text
客户特殊清单
```

字段：

```text
客户代码
业务员
客户简称
下单员
CCL特殊规则
PP特殊规则
PP小片特殊规则
通用特殊规则
非影响转码备注
```

必须保留的客户匹配字段：

```text
客户代码
业务员
客户简称
下单员
```

当前执行范围：

- `通用特殊规则`：必须抓取，优先级高，适用于该客户所有物料。
- `CCL特殊规则`：必须抓取，适用于 CCL。
- `非影响转码备注`：结构化为参考信息，不作为执行规则。
- `PP特殊规则`：暂不解析、不执行。
- `PP小片特殊规则`：暂不解析、不执行。

2026-07-08 补充执行字段口径：

本轮 CCL 规则只关注以下会直接影响当前营销转码品名的字段：

```text
胶系
基板厚度
铜箔规格
基板尺寸
胶水类别
铜箔类型+印字/非印字
基板级别
总/芯厚
```

以下字段本轮暂不转码，不进入当前 Agent 执行规则：

```text
组合结构
铜箔厂商
玻布厂商
配方代码
```

如果 `CCL特殊规则` 中涉及“订单备注、订单内容、客户订单字段”等订单语义理解，先不作为当前转码规则执行；后续单独设计订单语义理解处理。

## 4. 结构化草稿形成口径

草稿表保持原表有效客户行数，不把一个客户拆成多行。

当前草稿新增列：

```text
通用特殊规则_结构化
CCL特殊规则_结构化
非影响转码备注_结构化
待确认点
不纳入本次转码说明
结构化处理状态
本轮复核分类
本轮复核说明
本轮复核依据
本轮复核时间
```

当前草稿另有辅助说明 Sheet：

```text
待确认降噪说明
业务反馈处理说明
```

结构化状态：

```text
已结构化草稿
技术待支持
有待确认
仅参考信息
无可结构化内容
```

当前统计口径：

- `有待确认`：`待确认点` 不为空。
- `已结构化草稿`：结构化列中存在 `可执行草稿`，且无待确认点。
- `技术待支持`：业务规则和触发条件已明确，但当前 Agent 执行规则还缺尺寸覆盖、单边尺寸映射、物料编码条件、后缀位映射等技术能力；此类不再作为业务待确认问题。
- `仅参考信息`：结构化列或非影响备注中只有参考信息。
- `无可结构化内容`：没有可执行、没有参考、没有待确认。

## 5. 规则结构化字段建议

面向 Agent 可执行规则时，建议使用统一字段名。

常用覆盖字段：

```text
glue_code              胶系代码
thickness_rule         厚度/厚度换算规则
thickness_code         厚度代码
copper_code            铜箔规格代码
size_code              尺寸代码
glue_category_code     胶水类别代码
copper_type_code       铜箔类型/印字代码
grade_code             基板级别代码
tc_code                总厚/芯厚代码
struct_code            结构码
reference_note         参考备注
```

注意：`struct_code` 虽然是未来可执行字段，但 2026-07-08 业务口径已确认“组合结构本轮不考虑”，因此当前从草稿到 Agent 执行规则时暂不纳入。

当前不参与营销转码品名编码、只保留参考的字段：

```text
铜箔厂商
玻布厂商
配方代码
组合结构
排版结构/布号映射
PP规则
PP小片规则
订单语义规则
```

## 6. 参考逻辑和关联表

结构化草稿不能只看原表文本，需要结合项目已有规则逻辑。

### 6.1 胶系代码

参考：

```text
transcode_rules.xlsx / 胶系代码
transcode_engine.py / get_glue_code
```

用途：

- 判断客户特殊规则中的胶系是否已有标准映射。
- 判断是否需要用客户特殊规则覆盖胶系代码。

示例：

```text
NY2150=AS
NY3170HF=3C
```

可结构化为：

```text
条件胶系=NY2150 -> glue_code=AS
条件胶系=NY3170HF -> glue_code=3C
```

### 6.2 客户下单与胶系基板转换

参考：

```text
transcode_rules.xlsx / 客户下单与胶系基板转换
transcode_engine.py / apply_customer_order_rule
```

用途：

- 复核客户特殊规则中的胶系代码、基板级别、结构代码是否已有项目规则支撑。

已确认可参考的例子：

```text
珠海景旺：NY2150 -> AS
江西景旺：NY2150 -> 2S
景旺系：NY3150HCP -> RV
方正F7：NY2150 -> 2B/2C，结构 Y/Z
吉安满坤：NY2140 -> F1
江福昌发：NY2140 -> F1，NY1600 -> A2
```

注意：

```text
NY3150HC=RV/3H
```

如果没有选择条件，仍不能唯一确定 `RV` 还是 `3H`。但可以明确：

```text
RV -> 基板级别 A1
3H -> 基板级别 AL
```

### 6.3 编码规则

参考：

```text
transcode_rules.xlsx / 编码规则
transcode_engine.py / get_grade_code
transcode_engine.py / get_struct_code
```

用途：

- 复核基板级别代码。
- 复核结构码含义。

已确认口径：

- `TFT` -> `AT`
- 汽车板/车用/汽车专用 -> 汽车相关等级，通常为 `AC`
- `NY2140` 且厚度满足规则、铜厚不超过 1oz -> 可下 `F1`
- 湖北健鼎/超颖电子当前系统既有口径默认结构码 `E`，已确认可作为本次结构化草稿的可执行规则。

结构码注意：

```text
Y/Z
RV/3H
其他客户出现 E/H/I 且没有选择条件
```

这类多值如果缺少选择条件，不能强行唯一化。湖北健鼎/超颖电子例外：当前按系统既有逻辑默认 `E`，本次不新增 `H/I` 分支。

### 6.4 总芯厚转换

参考：

```text
transcode_rules.xlsx / 总芯厚转换
transcode_engine.py / calc_order_thickness
```

用途：

- 当特殊规则写明总厚/芯厚，或需要总芯厚转换时，按铜厚组合换算。

典型口径：

```text
全部芯厚 -> tc_code=C
全部总厚 -> tc_code=T
默认总厚，备注芯厚除外 -> 默认 T，命中芯厚备注时 C
默认芯厚，达到明确阈值需要转换 -> 按总芯厚转换表执行
默认总厚，低于明确阈值需要转换 -> 按总芯厚转换表执行
```

如果特殊规则已经写明触发条件，例如：

```text
基板厚度 < 0.8 需要进行芯总厚转换
基板厚度 >= 0.8 需要进行芯总厚转换
默认全部芯厚，基板厚度 >= 0.8 需要进行芯总厚转换
含铜是总厚且 < 0.8 时需要总厚转芯厚
```

则不应作为业务待确认，应结构化为：

```text
字段=基板厚度/总芯厚
覆盖字段=thickness_rule 或 tc_code
覆盖值=按总芯厚转换表执行
条件=原文明确阈值
状态=可执行草稿
依据=transcode_rules.xlsx/总芯厚转换 + transcode_engine.py/calc_order_thickness
```

只有特殊规则仅写“需要芯总厚转换”，但没有说明何时触发、以总厚还是芯厚为默认口径时，才保留待业务确认。

### 6.5 特殊需求旧 Sheet

参考：

```text
transcode_rules.xlsx / 特殊需求
transcode_engine.py / special_by_name、special_by_code
```

用途：

- 复核旧规则中已经存在的客户特殊口径。
- 作为新格式原表结构化的辅助依据。

使用原则：

- 新格式业务原表优先。
- 旧 `特殊需求` Sheet 只能作为辅助依据。
- 如果新旧规则冲突，必须标记待业务确认。

典型已辅助明确的规则：

```text
常熟敬鹏：0.71含以下芯厚，0.71以上总厚，FML为芯厚
奥士康/广东喜珍/森德科技：NY2140L无偏正公差 -> A，有偏正公差 -> S
深南系：排版结构 A=106 等属于参考信息，不纳入当前品名编码
```

### 6.6 尺寸规则

参考：

```text
transcode_engine.py / extract_size
transcode_engine.py / size_to_code
transcode_engine.py / get_special_size
```

通用尺寸编码规则：

```text
尺寸代码 = 宽度4位 + 长度4位
每边：整数2位 + 小数2位
```

例如：

```text
49*74 -> 49007400
74*49 -> 74004900
```

客户特殊尺寸规则：

```text
如果特殊需求明确写出 28*48=28504900，则按特殊需求原值执行。
```

注意：

- 系统旧 `get_special_size` 只识别“尺寸后跟 8 位码”的模式。
- 7 位码如果是业务明确写出的客户特殊映射，可以在草稿中保留原值。
- 如果规则已明确给出客户下单尺寸到厂内尺寸的映射，例如 `37*49=37.3*49.3`、`725.4*1244.6=28.56*49`，不应作为业务待确认；应标记为 `技术待支持`，后续由 Agent 支持尺寸覆盖。
- 如果只写单边尺寸映射，例如 `1888=74.3`、`2091=82.3`，也不应强行要求业务补 8 位尺寸码；应标记为 `技术待支持`，后续实现“单边替换后再组合生成尺寸码”。
- 如果引用外部表但项目内缺该表，例如“参考新美亚客户尺寸转换表”，仍需补充外部规则源，保留待业务确认或待资料补充。

### 6.7 铜箔规格和铜箔类型

参考：

```text
transcode_engine.py / extract_copper_spec
transcode_engine.py / copper_to_code
transcode_engine.py / get_copper_type_code
```

铜箔规格示例：

```text
H/H -> HH
J/J -> JJ
F/F -> FF
1/1 -> 11
R/R 如果业务写 R/R=F/F，则按客户特殊规则覆盖为 FF
```

铜箔类型示例：

```text
RTF  -> 特殊铜箔代码
RTF2 -> 特殊铜箔代码
HVLP/HVLP1 -> 特殊铜箔代码
HTE -> 常规铜，不作为特殊铜箔代码
有水印/无水印 -> 按印字/水印规则处理
```

已确认口径：

```text
订单未备注铜箔，默认 HTE -> 可按常规铜默认 W 处理
R/R=F/F 或 R/R=1.5/1.5 -> copper_code=FF
H/H=J/J -> copper_code=JJ
1/1=F/F -> copper_code=FF
1-/1-=K/K -> copper_code=KK
```

这类规则不应因为解析拆分异常而保留为业务待确认。

## 7. 健鼎/超颖板厚换算规则

业务补充表：

```text
超颖客户下单尺寸 特殊要求(1).xlsx
健鼎客户下单特殊要求(1).xlsx
```

主要使用 Sheet：

```text
健鼎超颖板厚换算
```

关键列：

```text
板厚
厂内规格
铜厚
规格中值 spec
公差Tolerance
上限
下限
```

已确认口径：

```text
客户规格 mil 板厚 + 铜厚 -> 厂内规格 mm
mil < 31  -> 芯厚
mil >= 31 -> 总厚
```

适用客户：

```text
超颖电子
湖北健鼎
```

不适用客户：

```text
无锡健鼎
```

示例：

```text
24mil + H/H -> 厂内规格 0.61mm
25mil + H/H -> 厂内规格 0.63mm
31mil + H/H -> 厂内规格 0.79mm，按总厚
```

当前项目代码里已有类似逻辑，但存在硬编码个人桌面路径问题：

```text
transcode_engine.py / _load_jd_cy_thickness_map
```

后续正式实现时，不能继续依赖个人桌面路径，应把换算表作为 Agent 专用规则源或受控规则文件。

## 8. 从草稿到 Agent 真正规则表

草稿表不是 Agent 最终执行表。它是业务确认稿。

建议形成 Agent 真正规则表时，至少包含以下字段：

```text
rule_id
enabled
priority
source_type
customer_code
sales_name
customer_name
order_user
material_scope
source_column
target_field
target_value
condition_glue
condition_thickness
condition_copper
condition_size
condition_keyword
condition_text
rule_explanation
confirmed_by
confirmed_at
version
remark
```

优先级建议：

```text
通用特殊规则 priority=10
CCL特殊规则 priority=20
参考信息 priority=90，不执行
```

执行范围：

```text
通用特殊规则 -> 全部物料
CCL特殊规则 -> CCL
PP/PP小片 -> 当前不执行
```

真正规则表只接收：

```text
结构化处理状态=已结构化草稿
或业务确认后从“有待确认”转为可执行的规则
```

不得接收：

```text
仍有待确认的多值规则
技术待支持但尚未实现的规则
涉及订单语义但尚未进入订单语义模块的规则
本轮明确不转的组合结构、铜箔厂商、玻布厂商、配方代码
仅参考信息
不纳入本次转码说明
PP/PP小片规则
```

## 9. 待确认分类

仍需业务确认的典型情况：

```text
同一字段多值但没有选择条件：RV/3H、Y/Z、其他客户的 E/H/I
外部转换表缺失：例如新美亚尺寸转换表
新旧规则冲突：例如旧规则与新表对铜箔覆盖不一致
编码规则表缺少目标代码：例如 AB 等级
同一条件上下边界冲突：例如 0.9mm 同时含以上/含以下
规则内部优先级冲突：例如同一客户同时写非汽车板和基板下汽车板
条件过于口语化且无法落到字段
```

2026-07-08 业务反馈后，上述分类需再结合本轮执行范围判断：

- `组合结构` 不再作为当前业务待确认，即使出现 `Y/Z`、`E/H/I`、`L/M/Q`、`CP/CN`，本轮也不要求业务确认，直接移入“不纳入本次转码说明”。
- `铜箔厂商`、`玻布厂商`、`配方代码` 不作为当前业务待确认。
- 涉及订单备注或订单字段语义的规则，不作为当前转码规则待确认，标记为后续订单语义处理。
- 如果业务反馈已经给出明确选择，例如景旺系 `NY3150HC=RV/3H` 选择 `RV`，则从待确认移入可执行草稿。

可以明确但要拆分的情况：

```text
NY-A1或NY-A2=AC
NY2150或NY2170=AC
订单有双面字样 -> RTF，否则 HTE
有偏正公差 -> S，无偏正公差 -> A
默认 HTE -> 常规铜 W
明确阈值下总芯厚转换 -> 按总芯厚转换表执行
NY2140 且厚度/铜厚满足条件 -> F1
```

业务明确但技术待支持的情况：

```text
完整尺寸映射：37*49=37.3*49.3、725.4*1244.6=28.56*49
单边尺寸映射：1888=74.3、2091=82.3
尺寸统一放大：全部默认下加大0.3尺寸
物料编码条件：631=芯厚、632=总厚，需要稳定读取物料编码字段
编码后缀位映射：J0J0F0需要对应到*后24-30位数
客户专用厚度映射：003=3mil=0.079
客户订单备注字段作为铜箔规格来源
```

可以明确排除的情况：

```text
铜箔厂商
玻布厂商
配方代码
排版结构/布号映射
非影响转码备注
PP/PP小片规则
```

## 10. 当前草稿状态

当前草稿已完成：

- 保留原表 219 行。
- 结合业务新格式原表进行结构化。
- 结合健鼎/超颖板厚换算表补充确认。
- 结合 `transcode_engine.py` 和 `transcode_rules.xlsx` 复核一批规则。
- 清理 `解析置信度不足` 表述。
- 修正状态统计口径。
- 2026-07-08 重新复核 69 行待确认，把已有规则表/代码可确定的规则从业务待确认中移出，把业务清楚但 Agent 暂缺执行能力的规则标记为 `技术待支持`。
- 在草稿表中新增 `待确认降噪说明` Sheet，记录每行调整前后状态、复核分类、复核依据。
- 2026-07-08 根据补充的 `业务反馈` 列再次复核，确认组合结构本轮不考虑、订单语义后续单独处理、新美亚尺寸表已补充、景旺系选择 `RV` 等口径。
- 在草稿表中新增 `业务反馈处理说明` Sheet，记录业务反馈导致的状态调整。
- 2026-07-08 根据川华兴宇补充反馈，确认 `0.9mm以下` 不含 0.9，等于 0.9 归入 `0.9mm（含）以上=A1`。
- 2026-07-08 根据黄石广合补充反馈，删除 `NY3150HF 全部按 AB 等级 NB 专用料号` 规则，不再作为待确认或执行规则。

当前统计：

```text
已结构化草稿：79
技术待支持：19
有待确认：0
仅参考信息：24
无可结构化内容：97
```

当前已无 `有待确认` 行。

注：

- 方正F7、广东世运、江苏瀚宇、九江明阳、深圳明阳、无锡健鼎等原待确认点主要是组合结构，本轮按业务反馈“不考虑组合结构”移出待确认。
- 江西景旺、龙川景旺、深圳景旺、泰国景旺已有业务反馈选择 `RV`，本轮移入可执行草稿。
- 无新美亚尺寸转换表已补充为 `develop0707/新美亚规格尺寸对照表.xlsx`；2026-07-09 已接入运行时，`631` 按表换算，`632` 按英寸尺寸 `*25.4` 四舍五入。
- 广东世运、鹤山世安等涉及订单备注第 5 码的汽车板识别，后续进入订单语义理解处理。
- 珠海乐健、赣州乐健业务反馈明确 `PP` 不考虑，只考虑基板；2026-07-09 已接入 CCL 基板级别规则：默认汽车板 `AC`，指定 `NY2150` 非汽车板条件为 `A1`。
- 万安裕维业务反馈确认“未标 TG 或 TG130 默认 NY2140”，当前改为默认胶系/胶系语义映射技术待支持。
- 川华兴宇补充反馈确认“以下的含去掉了”，因此 `NY2150/NY2170` 厚度 `>=0.9mm` 为 `A1`，厚度 `<0.9mm` 为 `AC`，本轮已移入可执行草稿。
- 黄石广合补充反馈确认“当胶系=NY3150HF时，全部按AB等级NB专用料号”需要删除，本轮已从待确认和执行规则中移除。

## 11. 后续维护步骤

后续如业务确认了待确认项，按以下步骤更新：

1. 在草稿表中定位客户行。
2. 修改对应结构化列。
3. 从 `待确认点` 中移除已确认内容。
4. 如果变为可执行规则，标记为 `状态=可执行草稿`。
5. 如果确认不参与转码，移动到 `不纳入本次转码说明` 或 `参考信息`。
6. 更新 `结构化处理状态`。
7. 更新统计说明。
8. 将本 MD 的“当前草稿状态”与“已确认口径”同步更新。
9. 只有业务确认后的可执行规则，才能进入 Agent 真正客户特殊规则表。

如果业务规则已经清楚但当前 Agent 尚不支持，应标记为 `技术待支持`，不要继续混在 `有待确认` 中。技术实现完成并回归通过后，再把对应行转为 `已结构化草稿` 或生成 Agent 真正规则表。

如果业务反馈确认某字段本轮不转，例如组合结构、铜箔厂商、玻布厂商、配方代码，应从 `待确认点` 移除，写入 `不纳入本次转码说明` 或复核说明，不再要求业务确认。

## 12. 代码开发约束

后续开发 Agent 规则读取逻辑时必须遵守：

- 不直接替换 `transcode_rules.xlsx` 的旧 `特殊需求` Sheet。
- 不影响普通转码功能。
- 只让营销转码 Agent 读取 Agent 专用规则源。
- 如果复用 `transcode_engine.py` 的共用能力，必须标明影响范围。
- 不继续使用个人桌面硬编码路径。
- 每次只开发一个功能点。
- 每次修改后必须回归测试。

建议正式落地顺序：

```text
1. 业务确认草稿
2. 生成 Agent 可执行规则表
3. 实现 Agent 专用规则加载器
4. 接入营销转码 Agent
5. 建立回归测试样本
6. 小范围验证
7. 再进入下一批规则优化
```

## 13. 变更记录

```text
2026-07-07
- 建立本文档。
- 记录业务原表 -> 结构化草稿 -> Agent 真正规则表的形成流程。
- 补充健鼎/超颖板厚换算表口径。
- 补充结合 transcode_engine.py 和 transcode_rules.xlsx 的复核逻辑。
- 补充当前草稿统计状态。
- 确认湖北健鼎/超颖电子结构码按系统既有逻辑默认 `E`，从待确认移入可执行草稿；当前不新增 `H/I` 分支。

2026-07-08
- 同步 `客户特殊规则结构化草稿_按原表行_20260707.xlsx` 的待确认降噪结果。
- 新增 `技术待支持` 状态，用于区分业务规则清楚但 Agent 暂缺执行能力的场景。
- 明确总芯厚转换：触发条件已写明时，按 `transcode_rules.xlsx / 总芯厚转换` 和 `transcode_engine.py / calc_order_thickness` 执行，不再作为业务待确认。
- 明确默认 HTE、R/R=F/F、NY2140 条件下 F1、AD/AH/AL/AP/AM/AC/AT 等已有编码规则支持的规则，不应保留为业务待确认。
- 更新当前统计：已结构化草稿 68、技术待支持 13、有待确认 17、仅参考信息 24、无可结构化内容 97。
- 补充剩余 17 条真正业务待确认清单。

2026-07-08
- 根据业务反馈列再次复核：组合结构、铜箔厂商、玻布厂商、配方代码本轮暂不转；涉及订单语义的 CCL 规则后续单独处理。
- 确认新美亚尺寸转换表已放入 `develop0707/新美亚规格尺寸对照表.xlsx`，从业务待确认改为技术待支持。
- 确认江西景旺、龙川景旺、深圳景旺、泰国景旺的 `NY3150HC=RV/3H` 选择 `RV`。
- 确认惠州泰和 `总厚1.2（含）以下=AT`。
- 确认万安裕维“未标 TG 或 TG130 默认 NY2140，TG150=2B，TG150+无卤素=3B”，改为默认胶系/语义映射技术待支持。
- 更新当前统计：已结构化草稿 77、技术待支持 19、有待确认 2、仅参考信息 24、无可结构化内容 97。
- 新增 `业务反馈处理说明` Sheet。

2026-07-08
- 根据川华兴宇补充反馈，“0.9mm以下”不含 0.9；边界改为 `>=0.9mm -> A1`、`<0.9mm -> AC`。
- 更新当前统计：已结构化草稿 78、技术待支持 19、有待确认 1、仅参考信息 24、无可结构化内容 97。

2026-07-08
- 根据黄石广合补充反馈，删除 `NY3150HF 全部按 AB 等级 NB 专用料号` 规则，不再需要 AB 编码确认。
- 更新当前统计：已结构化草稿 79、技术待支持 19、有待确认 0、仅参考信息 24、无可结构化内容 97。

2026-07-09
- 营销转码 Agent 后台接入无新美亚外部尺寸表：`631` 读取 `新美亚规格尺寸对照表.xlsx` 的 Thin Core 对照，`632` 按订单英寸尺寸 `*25.4` 四舍五入。
- 营销转码 Agent 后台接入珠海乐健、赣州乐健 CCL 基板级别规则；业务反馈明确 PP 不考虑，仅处理 CCL。
- 最终 Agent 机器规则数更新为 200，辅助映射启用数更新为 78，待接入规则 Sheet 降为 15。
- 本轮仍不修改前端页面，不修改普通转码规则表。
```


## 14. 2026-07-08 简化同步变更记录（本次更新）

本次根据用户最新反馈完成以下同步：

1. Excel 新增 `结构化草稿_简化`、`清洗日志`、`简化统计` 三个 Sheet。
2. 当前阶段继续保持“一客户一行”，不提前拆成最终 Agent 的“一条规则一行”。
3. 结构化规则只保留当前营销转码需要的 8 类字段：胶系、基板厚度、铜箔规格、基板尺寸、胶水类别、铜箔类型+印字/非印字、基板级别、总/芯厚。
4. 组合结构、铜箔厂商、玻布厂商、配方代码、PP规则、PP小片规则、订单语义规则、排版结构/布号映射均不进入当前可执行结构化规则。
5. `非影响转码备注` 只保留原文参考，不再形成 `非影响转码备注_结构化` 规则列。
6. 状态简化为 `可执行草稿 / 技术待支持 / 不纳入 / 无规则`。
7. 规则优先级统一为：客户订单明示 > CCL特殊规则 > 通用特殊规则 > 普通规则 > 默认规则。
8. 业务反馈列作为最高可信来源。
9. 订单语义模块暂不考虑，后续单独建设。
10. `清洗日志` 保留被移出的字段、处理动作和原因，避免信息丢失。

简化后统计：

```text
可执行草稿：66
技术待支持：19
不纳入：37
无规则：97
```

## 15. 2026-07-10 模型语义规则分流与维护SOP

### 15.1 草稿新增字段

`结构化草稿` 新增以下五列：

```text
CCL规则执行路径
CCL模型语义规则_候选
模型所需订单字段
模型规则确认状态
模型语义规则ID
```

执行路径只允许：

```text
标准规则
标准规则+模型语义
模型语义
不纳入
无规则
```

### 15.2 三类规则资产边界

```text
确定性规则：草稿确认后生成最终transcode_agent_rules.xlsx或辅助映射表，由规则引擎执行。
模型语义候选：保存在草稿“模型语义规则候选”Sheet，仅供离线JSON化和业务审核。
模型正式规则：只允许从业务已确认候选生成独立版本；当前尚未生成，运行时不得读取候选Sheet。
```

组合结构、铜箔厂商、玻布厂商、配方代码仍不纳入；订单/备注内容只提取为模型候选，不作为当前转码规则执行。

### 15.3 当前分流结果

```text
草稿客户行：219
模型语义候选：42
涉及客户：32
标准规则：53
标准规则+模型语义：28
模型语义：4
不纳入：34
无规则：100
```

### 15.4 业务新原表维护流程

```text
1. 版本化保存业务原表，先比较新增、修改、删除的客户行。
2. 只拆解本次8类CCL字段；忽略组合结构、厂商、配方和PP。
3. 可枚举、可计算、可查正式表的内容进入确定性规则草稿。
4. 需要订单备注、客户物料文本、缺失默认、排除集合或上下文理解的内容进入模型候选。
5. 候选先生成受控JSON并通过Schema、原文证据和输入字段校验。
6. 业务确认后才发布最终模型规则表；未确认候选不得参与运行时。
7. 生成规则版本，跑专项测试、P0和499条基线回归，再允许进入页面影子模式。
```

详细步骤以 `model_skills/customer-special-rule-maintenance/SKILL.md` 和 `references/维护SOP.md` 为准。

### 15.5 当前阶段结论

P2-1A 只完成草稿分流和维护流程。模型调用次数为 0，正式机器规则仍为 200 条，页面出码和现有评分门禁没有变化。

## 16. 2026-07-10 P2-1B 模型JSON化结果

模型参数：

```text
provider=DeepSeek
model=deepseek-v4-pro
base_url=https://api.deepseek.com
timeout_seconds=60
```

草稿新增：

```text
模型语义业务审核：42条候选的标准语义、目标字段、条件JSON、证据、状态、模型和业务确认列。
模型JSON化统计：模型版本、哈希、39/3/0统计、3条待确认问题和阶段回归结果。
```

最终统计：

```text
候选规则=42
程序校验通过=39
待业务确认=3
程序校验失败=0
结构化后可确定性执行=39
运行时需要模型=0
业务已确认=0
```

当前3条待确认：

```text
超颖电子“除汽车板外=AT”：需要明确汽车板依据哪个订单字段识别。
广华升鑫/深华升鑫“客户订单备注”：需要补充铜箔规格关键词和映射。
深同创鑫“客户订单备注”：需要补充铜箔规格关键词和映射。
```

P2-1B仍是离线维护阶段。程序通过的39条不会自动进入运行时；业务确认后，P2-1C才生成独立最终模型语义规则表。

## 17. 2026-07-10 P2-1C 最终语义规则发布流程

本轮用户确认：3条待业务确认继续保留，其余39条确定转换规则进入下一步。

最终规则主表：

```text
文件：营销转码Agent最终模型语义规则表_20260710.xlsx
正式规则Sheet：39条TSR规则
待确认Sheet：3条MSR候选
版本说明Sheet：批准依据、模型、active版本和回归结果
```

版本目录：

```text
storage/rules/transcode_semantic/versions/<version>/transcode_semantic_rules.xlsx
storage/rules/transcode_semantic/versions/<version>/transcode_semantic_rules.json
storage/rules/transcode_semantic/versions/<version>/manifest.json
```

当前 active 语义版本：

```text
transcode_semantic_rules_20260710_153950_441894
```

发布校验顺序：

```text
1. Excel正式Sheet只允许业务确认=确认、启用=是、执行方式=结构化后可确定性执行。
2. 检查规则ID和来源候选ID唯一。
3. 检查业务字段、目标字段、条件JSON和原文证据。
4. 生成机器JSON；待业务确认Sheet不得进入JSON。
5. 生成manifest并记录xlsx/json哈希。
6. 校验版本后才能设为active；回滚通过激活历史版本完成。
7. active只代表语义资产版本，P2-2前不接正式转码运行时。
```

本轮验收：39条正式、3条隔离；版本加载、回滚、哈希拦截、P0和499条回归通过。

## 18. 2026-07-10 P2-2 运行时影子验证流程

```text
1. 页面仅上传订单表，后台从active语义版本加载最终JSON，不读草稿表。
2. 先执行原有确定性转码，保留formal_code、candidate_code、field_evidence和overall_score。
3. 由输入适配器提供订单规格、订单备注、客户规格、客户料品名称、客户物料编码，以及规格解析得到的胶系、厚度、铜箔规格。
4. 语义条件评估输出命中、未命中、缺少输入、条件错误。列不存在与列存在但值为空必须区分。
5. 将评估写入独立“模型语义影子证据”Sheet，不回写正式码或评分。
6. 对同一订单分别用shadow=off和shadow运行，正式结果必须逐行相同。
7. 运行专项、P0、pytest、页面样本和历史订单回归后，才能将影子证据交业务验收。
```

当前结果：39条正式规则可影子评估；3条待业务确认仍未进入JSON；10行页面样本评估8条、缺少输入2条；499行出码准确率仍为89.29%。

发布前静态审计的新门禁：语义规则不得同时存在互斥AND条件；不得将多个条件到不同目标的分支压成一条；可执行规则的目标值不得为空。当前已标记13条待P2-2B清洗，完成前影子结果不得进入评分。
